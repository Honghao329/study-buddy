.login-page {
	min-height: 100vh;
	background: linear-gradient(180deg, #4A90D9 0%, #6BB5F0 35%, #f5f6fa 35%);
	padding-bottom: 60rpx;
}

/* 顶部装饰圆 */
.login-top { position: relative; height: 0; }
.login-circle { position: absolute; border-radius: 50%; opacity: 0.1; background: #fff; }
.c1 { width: 200rpx; height: 200rpx; top: -60rpx; right: -40rpx; }
.c2 { width: 120rpx; height: 120rpx; top: 40rpx; left: 30rpx; }
.c3 { width: 80rpx; height: 80rpx; top: 20rpx; right: 120rpx; }

/* Logo区域 */
.login-logo {
	padding: 100rpx 0 60rpx;
	text-align: center;
}
.logo-icon {
	width: 140rpx; height: 140rpx; border-radius: 36rpx;
	background: rgba(255,255,255,0.2);
	display: flex; align-items: center; justify-content: center;
	margin: 0 auto 24rpx;
	backdrop-filter: blur(10px);
}
.logo-title {
	font-size: 44rpx; font-weight: bold; color: #fff;
	letter-spacing: 4rpx; margin-bottom: 12rpx;
}
.logo-desc {
	font-size: 26rpx; color: rgba(255,255,255,0.85);
}

/* 登录表单卡片 */
.login-form {
	margin: 0 40rpx;
	background: #fff;
	border-radius: 24rpx;
	padding: 48rpx 40rpx;
	box-shadow: 0 8rpx 40rpx rgba(0,0,0,0.08);
}

/* 头像选择 */
.avatar-section {
	text-align: center;
	margin-bottom: 40rpx;
}
.avatar-wrap {
	width: 160rpx; height: 160rpx; border-radius: 50%;
	margin: 0 auto 16rpx;
	overflow: hidden;
	border: 4rpx solid #EBF3FC;
}
.avatar-img {
	width: 100%; height: 100%;
}
.avatar-placeholder {
	width: 100%; height: 100%;
	background: #f0f4ff;
	display: flex; align-items: center; justify-content: center;
}
.avatar-tip {
	font-size: 24rpx; color: #999;
}

/* 输入框 */
.input-group {
	margin-bottom: 16rpx;
}
.input-label {
	display: flex; align-items: center; gap: 8rpx;
	font-size: 26rpx; color: #666; margin-bottom: 12rpx; font-weight: 500;
}
.login-input {
	width: 100%; box-sizing: border-box;
	border: 2rpx solid #e8eef6; border-radius: 16rpx;
	padding: 24rpx; font-size: 30rpx; background: #f8fbff;
	transition: border-color 0.3s;
}

/* 提示 */
.login-hint {
	text-align: center; margin-top: 24rpx;
	font-size: 24rpx; color: #bbb;
}
